{
  "name": "fake-db",
  "version": "0.0.7",
  "description": "Generate a db with fake data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EMS",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "mongoose": "^5.7.5"
  }
}
